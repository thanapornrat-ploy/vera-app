# SPEC.md — ระบบ "Vera"

## ระบบนี้คืออะไร
เว็บแอพมือถือส่วนตัว 2 พาร์ท: (1) ตั้งเป้าหมายพัฒนาตัวเอง เช็คอินรายวันพร้อมบันทึกอารมณ์/พลังงาน/รูป
(2) บันทึกรายจ่ายรายวันแยกหมวด ตั้งงบเตือน ไม่มี server กลาง ข้อมูลอยู่ในเครื่องผู้ใช้เท่านั้น

## ไฟล์ของระบบ
- `index.html` = showcase โชว์ภาพรวมทุกหน้าจอ (ให้พี่ปุ้ยดูก่อนว่าหน้าตาเป็นยังไง)
- `app.html` = แอพจริงที่ใช้งานได้ (ต่อ localStorage/IndexedDB จริง)
- `manifest.json`, `sw.js` = ไฟล์ PWA (ติดตั้งขึ้นหน้าจอโฮมมือถือได้)

## เมนูในระบบ (5 เมนูหลัก + หน้าตั้งค่า)
1. **วันนี้ (Today)** — เป้าหมายของวันนี้ทั้งหมด + ป็อปอัพเช็คอิน
2. **เป้าหมาย (Goals)** — สร้าง/แก้ไข/ปิดเป้าหมาย ดูรายละเอียดแต่ละอัน
3. **สรุปเดือน (Monthly)** — ปฏิทิน heatmap หลายสีตามเป้าหมาย
4. **อารมณ์ (Mood)** — สรุปกราฟอารมณ์/พลังงานย้อนหลัง
5. **การเงิน (Money)** — บันทึกรายจ่าย + ดูงบแต่ละหมวด
- **ตั้งค่า (Settings)** — เข้าถึงจาก topbar ไม่ใช่ bottom nav: Export/Import ข้อมูล, ตั้งเวลาป็อปอัพเช็คอินตอนเช้า, เปิด/ปิดแจ้งเตือน, ติดตั้งเป็น PWA

## ข้อมูลในระบบ

### `localStorage` คีย์ `vera_data`
```
goals: [{
  id, name, colorId (1-8), goalType ("habit" | "milestone"),
  frequencyPerWeek (เช่น 5), minutesPerDay, steps (ข้อความขั้นตอนคร่าว ๆ),
  startDate, endDate (optional), active (bool), createdAt
}]

checkins: [{
  id, goalId, date (YYYY-MM-DD), done (bool),
  stressLevel (0-3: เศร้า/เครียด/เหนื่อย), energyLevel (0-3: energy/happy/positive),
  comment (ข้อความ, optional), photoId (อ้างอิงไป vera_photos, optional),
  createdAt
}]

expenses: [{
  id, date, category (id จากหมวดคงที่), amount (บาท), note (optional), createdAt
}]

budgets: [{ category, monthlyLimit }]

settings: { morningPopupHour, notificationsEnabled }
```

### `IndexedDB` ฐานข้อมูล `vera_photos`
```
photos: { id, blob (รูปภาพ), createdAt }
```

### หมวดค่าใช้จ่ายคงที่ (8 หมวด)
skincare · ของใช้ประจำวัน · เสื้อผ้า/เครื่องประดับ · ยา-วิตามิน · ค่าเรียนอบรม · อาหาร/กาแฟ · entertainment · อื่นๆ

## กติกาการไหลของข้อมูล (หัวใจของระบบ)
- เปิดแอพครั้งแรกของวัน → เช็คทุกเป้าหมาย `active` ว่าวันนี้มี record ใน `checkins` แล้วหรือยัง ถ้ายัง → เด้งป็อปอัพเช็คอินทีละเป้าหมาย (ไม่ยัดทุกอันมาพร้อมกัน)
- 1 เป้าหมาย = เช็คอินได้ 1 ครั้ง/วัน กดซ้ำ = แก้ไขของเดิม ไม่สร้างซ้ำ
- Streak คำนวณจาก `checkins.done = true` ติดต่อกันของเป้าหมายนั้น ๆ ตามความถี่ที่ตั้งไว้ (เช่น 5 วัน/สัปดาห์ ไม่ต้องทำติดกันทุกวันปฏิทินก็นับ streak ได้ถ้าเข้าเป้าตามสัปดาห์)
- หน้า "สรุปเดือน" อ่านจาก `checkins` ทั้งเดือน วันไหนมี checkin ของเป้าหมายไหน = จุดสี/แท่งสีของเป้าหมายนั้นบนวันนั้น (วันเดียวทำได้หลายเป้าหมาย = หลายสีในวันเดียว)
- หน้า "อารมณ์" อ่าน `stressLevel` / `energyLevel` จากทุก checkin เรียงตามวันที่ ทำเป็นกราฟเส้น/แท่งย้อนหลัง (7 วัน / 30 วัน)
- รายจ่ายบันทึกแล้ว รวมยอดต่อหมวดต่อเดือนทันที เทียบกับ `budgets[category].monthlyLimit` — ถึง 80% ของงบ = สีเหลืองเตือน, เกิน 100% = สีแดง
- ลบเป้าหมาย/รายจ่าย = ลบจริง (personal use ไม่ต้องมี soft-delete ซับซ้อน) แต่ต้องมี confirm popup ก่อนลบเสมอ

## หน้าจอแต่ละเมนู (ย่อ)
- **วันนี้:** การ์ดเป้าหมายวันนี้ (สีของเป้าหมาย, ทำแล้ว/ยัง), popup เช็คอิน (ทำแล้ว/ยัง → ถ้าทำแล้ว ถามระดับเศร้า/เครียด/เหนื่อย 0-3, energy/happy/positive 0-3, comment, แนบรูป optional)
- **เป้าหมาย:** ลิสต์เป้าหมาย active/ปิดแล้ว, ปุ่ม "+เป้าหมายใหม่" (ฟอร์ม: ชื่อ, สี, ความถี่/สัปดาห์, นาที/วัน, ระยะเวลา, ขั้นตอนคร่าว ๆ), แตะดู progress/streak รายเป้าหมาย
- **สรุปเดือน:** ปฏิทิน heatmap สลับเดือนได้ ตำนานสี (legend) ผูกกับชื่อเป้าหมาย แตะวันดูรายละเอียดว่าทำอะไรบ้างวันนั้น
- **อารมณ์:** กราฟ stress/energy ย้อนหลัง, ค่าเฉลี่ยสัปดาห์นี้เทียบสัปดาห์ก่อน, ลิสต์ comment ล่าสุดพร้อมรูป (ถ้ามี)
- **การเงิน:** ปุ่ม "+รายจ่าย" (จำนวนเงิน, หมวด, วันที่, โน้ต), สรุปยอดใช้ต่อหมวดเทียบงบ (progress bar), ตั้ง/แก้งบแต่ละหมวด, ลิสต์รายจ่ายล่าสุด
- **ตั้งค่า:** Export ข้อมูลเป็นไฟล์ .json, Import กลับ, ตั้งเวลาป็อปอัพเช้า, ปุ่มขอ permission แจ้งเตือน + คำแนะนำ "เพิ่มไปหน้าจอโฮม"
