# CLAUDE.md — Vera (เวร่า)

## โปรเจกต์นี้คืออะไร
"Vera" คือเว็บแอพส่วนตัว (ไม่ใช่งานบริษัท) ของพี่ปุ้ย สำหรับติดตามการเติบโตของตัวเอง เล่นบนมือถือเป็นหลัก
มี 2 พาร์ท:
1. **Better Self** — ตั้งเป้าหมายที่อยากบรรลุ (เช่น Weight Training 5 วัน/สัปดาห์) เช็คอินทุกวันพร้อมบันทึกอารมณ์/พลังงาน + รูปภาพ
2. **การเงิน** — บันทึกรายจ่ายรายวัน แยกตามหมวด ตั้งงบต่อเดือน เตือนเมื่อใกล้เกิน

ผู้ใช้งาน: พี่ปุ้ยคนเดียว ไม่มีระบบ login หลายคน ไม่มี server กลาง

## ศัพท์ที่ใช้ในแอพ
- **Goal (เป้าหมาย)** = สิ่งที่อยากทำให้สำเร็จ มีความถี่/ระยะเวลา/สีประจำตัวของตัวเอง
- **Check-in (เช็คอิน)** = การกดบันทึกในแต่ละวันว่าทำเป้าหมายนั้นหรือยัง พร้อมอารมณ์+comment+รูป (optional)
- **Streak** = จำนวนวันติดต่อกันที่ทำสำเร็จ
- **Mood log** = บันทึกระดับเศร้า/เครียด/เหนื่อย (0-3) และ energy/happy/positive (0-3) ที่แนบมากับทุก check-in
- **Expense (รายจ่าย)** = 1 รายการค่าใช้จ่าย มีหมวด/จำนวนเงิน/วันที่/โน้ต
- **Budget (งบ)** = วงเงินที่ตั้งไว้ต่อหมวดต่อเดือน

## กฎเหล็กของแอพ (ห้ามฝ่าฝืน)
- ข้อมูลทั้งหมด (เป้าหมาย, เช็คอิน, อารมณ์, รายจ่าย, รูปภาพ) เก็บ **ในเครื่อง/เบราว์เซอร์เดียวเท่านั้น** ห้ามส่งออกไปที่ server ภายนอกใด ๆ (privacy — ข้อมูลอารมณ์และรูปเป็นเรื่องส่วนตัวมาก)
- ปิด-เปิดเบราว์เซอร์ หรือรีเฟรชแล้ว **ข้อมูลต้องไม่หาย**
- ทุกเป้าหมายที่ active ต้องมีป็อปอัพให้เช็คอินได้ทุกวัน (ดูหมายเหตุเรื่อง "ป็อปอัพตอนเช้า" ใน SPEC.md — เว็บแอพธรรมดาเด้งได้เฉพาะตอนเปิดแอพ ถ้าอยากให้เตือนตอนไม่ได้เปิดแอพต้องติดตั้งเป็น PWA + ขออนุญาต Notification)
- งบ (budget) แต่ละหมวด: เมื่อยอดใช้จริงถึง ~80% ของงบ ต้องมีสัญญาณเตือน (สี/ข้อความ) ก่อนที่จะเกิน
- ตัวเลขเงินทุกตัวต้องคำนวณจากรายการที่บันทึกจริงเท่านั้น ห้ามใส่เลขสมมติ
- ออกแบบมือถือก่อนเสมอ (mobile-first) ปุ่มใหญ่พอกดด้วยนิ้วโป้ง ใช้งานง่ายแม้ไม่ถนัดเทคโนโลยี
- `<เดาให้ — แก้ได้>` ปุ่ม Export/Import ข้อมูลเป็นไฟล์ (JSON) ใน หน้าตั้งค่า — เพิ่มไว้เป็นทางเลือก backup มือ (พี่ปุ้ยเลือก "เก็บในเครื่องเดียวพอ" แล้ว แต่ปุ่มนี้กันเหตุเผื่อเปลี่ยนเครื่อง ไม่ได้บังคับใช้ ลบออกได้ถ้าไม่ต้องการ)

## Design System — สกิน "Soft Bloom" (ทุกหน้าจอต้องตามนี้)
- **โครงหน้าจอ (มือถือก่อน):** ไม่มี sidebar — ใช้ **bottom navigation bar** คงที่ด้านล่าง 5 ปุ่ม
  (วันนี้ 🌤️ · เป้าหมาย 🎯 · สรุปเดือน 📅 · อารมณ์ 💗 · การเงิน 💸) + topbar บาง ๆ ด้านบนโชว์ชื่อหน้าปัจจุบัน
  หน้าจอกว้างขึ้น (แท็บเล็ต/เดสก์ท็อป) ให้ bottom nav ขยับเป็นแถบด้านซ้ายแคบ ๆ ได้ แต่ค่าเริ่มต้นออกแบบเพื่อมือถือ
- โทนสี: พื้นหลัง #fdf7f5 (ครีมอุ่น) · การ์ดขาว #ffffff เงานุ่ม · ตัวหนังสือหลัก #3a2e35 · ตัวรอง #8a7680
  · accent หลัก #d46a8f (rose) · accent รอง #a78bda (lavender, ใช้กับพาร์ทการเงิน)
  · สำเร็จ #4caf82 · เตือน #e0a63f · เกินงบ/อันตราย #e2596b
- การ์ดมุมโค้ง 18px เงานุ่มบาง ปุ่มหลักมุมโค้งเต็ม (pill shape)
- สีประจำเป้าหมายแต่ละอัน: ให้ผู้ใช้เลือกจากพาเลท 8 สีพาสเทลตอนสร้างเป้าหมาย ใช้สีเดียวกันทุกที่ที่อ้างถึงเป้าหมายนั้น (ปฏิทินสรุปเดือน, การ์ดเป้าหมาย, กราฟ)
- ฟอนต์: "Anuphan" เนื้อหา · "Bai Jamjuree" หัวข้อ/ตัวเลขเด่น (Google Fonts)
- ตัวเลขเงินชิดขวา ลูกน้ำหลักพัน มี "฿"
- ป็อปอัพเช็คอิน/มู้ด ใช้การ์ดลอยกลางจอ (modal) ปุ่มใหญ่ แตะง่าย เลือกระดับ 0-3 เป็นไอคอนหน้า/แท่งสี ไม่ใช่ dropdown

## กติกาเทคนิค
- แอพหลักไฟล์เดียว `app.html` · showcase ไฟล์เดียว `index.html` — เปิดได้เลยไม่ต้องต่อเน็ต (ยกเว้นโหลดฟอนต์)
- ห้ามใช้ไลบรารี JavaScript ภายนอก (Google Fonts ใช้ได้)
- ข้อมูลทั่วไป (เป้าหมาย/เช็คอิน/อารมณ์/รายจ่าย/งบ) เก็บใน `localStorage` คีย์ `vera_data`
- รูปภาพเก็บใน `IndexedDB` ฐานข้อมูล `vera_photos` (เพราะรูปเยอะขึ้นเรื่อย ๆ localStorage เก็บได้จำกัดไม่กี่ MB) — เช็คอินแต่ละอันเก็บแค่ id อ้างอิงไปยังรูปใน IndexedDB
- ทำเป็น PWA เบื้องต้น (manifest.json + service worker ง่าย ๆ) เพื่อให้ "เพิ่มไปหน้าจอโฮม" ได้ และขอ permission แจ้งเตือนได้ — แต่ต้องอธิบายในแอพตรง ๆ ว่าถ้าไม่กด "เพิ่มไปหน้าจอโฮม" + อนุญาตแจ้งเตือน จะไม่มีการเด้งเตือนตอนไม่ได้เปิดแอพ (ข้อจำกัดจริงของเว็บแอพ ไม่ใช่บั๊ก)
- ทุกตารางรายจ่าย/เช็คอินมีปุ่ม "ออกรายงาน CSV" ได้ (ทางเลือก)
- UI ภาษาไทยทั้งหมด
- ห้ามฝังข้อมูลส่วนตัวจริงของพี่ปุ้ยลงในโค้ดตัวอย่าง/mock data

## วิธีทำงาน
- ทำทีละ TASK ตาม TASKS.md เท่านั้น จบแต่ละก้อนให้บอกวิธีทดสอบที่ทำเองได้จริงบนมือถือ/เบราว์เซอร์
- พิสูจน์ก่อนบอกว่าเสร็จ (เปิดจริง กดจริง ไม่ใช่แค่เขียนโค้ดแล้วบอกว่าน่าจะทำงาน)
- ห้ามแก้ 3 ไฟล์นี้ (CLAUDE.md / SPEC.md / TASKS.md) เองโดยไม่ได้รับอนุญาตจากพี่ปุ้ย
- เมื่อถึง TASK เรื่อง deploy ให้อธิบายทีละขั้นตอน แบบไม่ใช้ศัพท์เทคนิค เพราะพี่ปุ้ยไม่ถนัดสายเทคนิค (git/GitHub/Cloudflare Pages ต้องสอนทีละคลิก)
- โปรเจกต์นี้เป็นของส่วนตัว **ห้าม**ใช้ GitHub org หรือ git identity ของบริษัท (rattanaphaiboon) เด็ดขาด ให้ใช้บัญชี/identity ส่วนตัวแยกต่างหาก
